const pk = require("@portkey/client");

describe("Check", () => {
  describe("Steps/Lint", () => {
    it("should be the first step", () => {
      expect(job.steps[0].name).toEqual("Lint");
    });
    it("should start by ", () => {});
  });
});
